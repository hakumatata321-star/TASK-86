{
  "name": "district-portal-fe",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend unit tests for district-portal JS utilities (app.js, map.js)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.1.3",
    "@vitest/coverage-v8": "^1.6.0"
  }
}
